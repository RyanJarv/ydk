# D-001: Store the purpose model in repo-local configuration

`ydk` keeps its model, graph, and anchors in `.ydk/` so the project's purpose evolves in the same review flow as the code.

This makes purpose changes visible, versioned, and easy to validate in CI.
